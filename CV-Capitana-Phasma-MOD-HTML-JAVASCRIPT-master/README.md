# CV de la capitana Phasma

Para arrancar el proyecto instala http-server:

```shell
npm install http-server -g
```
Después ejecuta el comando `http-server` desde la terminal en la carpeta donde tengas el proyecto.

